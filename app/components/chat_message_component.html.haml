%section.message{id: "message-#{id}", data: {controller: 'deletable'}}
  .balloon
    .nes-balloon.from-right
      %p
        = message.html_safe
  .avatar
    %span.user{style: "color: #{user_color}"}= user
    %img{src: avatar_url}
